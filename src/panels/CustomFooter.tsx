import { Box } from "@mui/material";

/** this component shows a static footer with my basic information */
export default function CustomFooter(){
    return (<Box width="1" sx={{display: 'flex'}}>
        asdf
    </Box>);
}